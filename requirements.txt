Flask==3.0.3
gunicorn==21.2.0
opencv-python-headless==4.10.0.84
numpy==1.26.4
reportlab==4.2.2

# Hardware / drone integration (install on Raspberry Pi / with hardware attached).
# Omit on laptop — app auto-detects absence and falls back to simulation mode.
picamera2==0.3.20; platform_machine == "aarch64" or platform_machine == "armv7l"
pymavlink==2.4.41

# Dev / testing
pytest==8.2.2
